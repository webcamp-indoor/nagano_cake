<div class="container mt-5">
  <div class="row">
    <div class="col-2">
      <%= render "genre", genres: @genres%>
    </div>
    <div class="col-10">
      <h2 class="text-center mb-5">商品一覧 (全 <%= @items.total_count %> 件)</h2>
      <%= render "public/items/card", items: @items %>
      <div class="mt-4 mb-4 justify-content-center d-flex">
        <%= paginate @items %>
      </div>
    </div>
  </div>
</div>